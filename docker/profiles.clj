{:auth {:repository-auth
        {#"https://clojars.org/repo" {:username :env :password :env}}
        {#"https://repo.clojars.org" {:username :env :password :env}}
        {"clojars" {:username :env :password :env}}}}
